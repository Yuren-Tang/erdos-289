# Dependency / last-consumer ledger and hostile review checklist — repaired packet

This file is normative for the repaired blind review.  The previous independent review found a genuine E4→E5→E6 quantifier failure, an incorrect `ΛB` transition cutoff, an omitted Prefix centre `α_B`, and a resource-ordering defect.  All four are repaired explicitly below.

# 1. Corrected dependency skeleton

- `E1` depends only on elementary arithmetic of `Q/Z`.
- `E2` depends only on the concrete physical-state definitions and finite Set-level pullback/image calculus.
- `G1` is the explicit grade-neutral reciprocal identity plus finite remoteness induction.
- `G2` is the elementary Chebyshev/lcm-binomial argument.
- `G3` is the polynomial restricted-fold theorem.
- `G4` is the quota corollary of the external Haxell theorem.
- `E3` depends on `E1,G2` and elementary congruence/divisor estimates.
- `E4` depends on `E1,E2,E3,G1,G2` plus the finite Prefix seed certificate.
- `E5` depends on `E1,E3,G3`; **it does not depend on a selected WideStart witness or survivor row**.
- `E6` depends on theorem-level constants from `E4`, the universal `E5` profile, `E1`, and `G2`.
- `E7` depends on `E2,E3,E4,E5,G4`; this is the first and last place an actual finite Haxell packing is chosen.
- `E8` depends on `E1,E2,E6,E7` and pullback stability of surjections.

In particular:

- there is no selected-WideStart edge `W_B -> E5`;
- there is no actual packing point in E5 or E6;
- no probability/LLL, MasterSlab, DirectLTAR, donor/predecessor, filtered-lifting, or opposite-coset Tail dependency occurs;
- no Lean theorem substitutes for a missing human proof.

# 2. Repaired quantifier order

The load-bearing order is:

1. E3 fixes the deterministic original rows `A_Q^{orig}`, late lower density constant `κ`, conflict bound, and resource series.
2. E4 proves theorem-level constants
   \[
   \exists\eta>0\ \exists\sigma>0\ \forall L=o(B/\log B)\ \exists B_0(L)\ \forall B\ge B_0(L)\ \exists W_B.
   \]
3. G4 fixes a theorem-level quota fraction `ρ_P` from the conflict bound.
4. Choose once and for all
   \[
   0<\rho<\kappa\sigma\rho_P.
   \]
5. **Before any WideStart witness is selected**, E5 defines
   \[
   Adm_\rho(Q)=\{A\subseteq A_Q^{orig}:|A|\ge\rho Q/\log Q\}
   \]
   and takes finite extrema over this universal family to obtain `d_ρ,m_ρ`.
6. E6 defines from this profile
   \[
   R(B)=A(\Lambda^2B)+N(\Lambda^2B)=o(B/\log B).
   \]
7. Any externally prescribed lower bound `B_†` may now be imposed.  Choose `B` beyond `B_0(R)`, `B_†`, and all scalar onsets; only then select one E4 WideStart witness `W_B`.
8. This witness determines `Surv_{W_B}` and the full centre
   \[
   \gamma_B=\alpha_B+\beta_B.
   \]
9. Run the scalar first-hit construction using the already-fixed profile.  E1 cofinality kills `γ_B` after a finite cutoff.
10. Only after a finite scalar horizon is fixed does E7 choose one Haxell packing.  Its row projections satisfy
   \[
   |Pack(Q)|\ge\kappa\sigma\rho_P Q/\log Q>\rho Q/\log Q,
   \]
   so they belong to the already-analysed universal `Adm_ρ(Q)`.

Any proof that computes `d_ρ,m_ρ` from `Surv_{W_B}` or after observing a packing point is invalid.

# 3. Corrected scalar transition check

Sponsors satisfy
\[
Q/\Lambda^2<q\le Q/\Lambda.
\]
If a sponsor lies at or before the start `B`, then
\[
q\le B\quad\Longrightarrow\quad Q<\Lambda^2B.
\]
Therefore the complete pre-sponsor transition is controlled through `Λ^2B`, not `ΛB`.  The startup reserve must be
\[
R(B)=A(\Lambda^2B)+N(\Lambda^2B).
\]
Check that this remains `o(B/log B)` and really covers every demand plus all negative width variation during the transition when positive increments are pessimistically discarded.

# 4. Centre and resource ordering

The WideStart target is
\[
Tor_{H_B}(\alpha_B+\beta_B)\times I_B.
\]
The full class
\[
\gamma_B=\alpha_B+\beta_B
\]
must be transported through the endpoint filtration.  No argument is permitted to drop `α_B`.  E1 cofinality must be applied to `γ_B` itself.

Resource smallness must be fixed before scalar first-hit.  Let `η` be the E4 theorem-level margin and choose
\[
B_{res}\quad\text{with}\quad\sum_{Q>B_{res}}roleMass(Q)<\eta.
\]
Run E6 with its arbitrary lower-bound parameter `B_†=B_res`.  The selected WideStart base then satisfies `B≥B_res` before any requested grade `k` or first-hit horizon is chosen.  E7 may not move the base after seeing a horizon.

# 5. Last-consumer ledger

| Datum / coordinate | Created in | Last consumer | Must be absent afterwards |
|---|---|---|---|
| carrier/sign/inverse coordinates | E3 | internal E3 transversality/fibre/support proofs | yes |
| `Q^2/log Q` support scale | E3 | E4 future-footprint estimate | yes after E4 |
| distinguished atom centre `Qk` | E3 | conflict incidence / E4-E7 conflict use | yes after E7 |
| bridge arithmetic bases and lcm moduli | E4 | bridge coverage/separation/footprint | yes at E4 boundary |
| finite seed numerical coordinates | E4 certificate | seed verification | yes at E4 boundary |
| neutral-cube individual coordinates | E4 | Hamming pullback and future subdensity | yes at E4 boundary |
| full WideStart centre `γ_B=α_B+β_B` | E4/E6 | E6 centre-vanishing and E8 target | yes after terminal target pullback |
| simple-value image, rich/poor route, heavy fibre | E5 | proof of universal rectangle profile | yes at E5 boundary |
| `d_ρ,m_ρ` | E5 | E6 scalar dynamics and E7 realization | yes before E8 |
| scalar sponsor history | E6 | proof `Term_k≠∅` | yes before terminalization |
| actual Haxell packing | E7 | finite physical lift | yes at E7 boundary |
| n-ary compatibility factorization | E7 | E2 strictification | yes at E7 boundary |
| `roleMass` and summability | E3 | E7 `W<2` bound | yes at E7 boundary |
| finite horizon `X` | E6/E7 | E8 pullbacks | yes after target-point pullback |
| target `(0,k)` | E8 | literalization | public conclusion |

# 6. Finite-solution / no-section checks

Check that the proof uses whole finite solution objects rather than coherent selected representatives in:

- distinct simple-value occurrence lifting in E5;
- heavy-fibre `(p-1)`-subsets in E5;
- Hamming-weight matching in E4;
- the terminal target-point pullback in E8.

# 7. Arithmetic / asymptotic hostile checks

Please independently verify:

1. bounded transversality exceptions from `ℓb^2≡±1 (mod p^e)`;
2. coefficient multiplicity `O(1)`;
3. exact simple-value fibre bound `≤Q/p` after deterministic deduplication;
4. global atom-centre recovery/injectivity;
5. prime and non-prime `roleMass` convergence;
6. Prefix bridge subset-sum coverage and fixed-grade property;
7. future bridge footprint `O(sqrt(log Q) log log Q)`;
8. ragged Prefix grade spread is the fixed finite seed spread;
9. E5 image-poor implication `p=O((log Q)^2)` and `p-1` equal-value occurrences for every composite current;
10. cumulative composite debt `O(sqrt X log^3X)=o(X/log X)`;
11. earlier sponsor existence in `(Q/Λ^2,Q/Λ]`;
12. corrected startup reserve through `Λ^2B`;
13. E5 profile independence from the selected WideStart witness;
14. cofinal annihilation of the full centre `α_B+β_B`;
15. resource-tail base fixed before scalar first-hit.

# 8. Physical / categorical hostile checks

Please verify:

1. the Haxell independent pool really lies in the genuine finite n-ary physical multiplication domain;
2. survivor rows are compatible with every branch of the selected WideStart response;
3. E5 is target-level only for arbitrary dense subrows of the original row; physical union is asserted only in E7 for the actual independent packing;
4. E2 strictification is used only after `P_n -> D_n` factorization;
5. every finite-horizon response covers the whole target fibre/interval;
6. `roleMass` is consumed in E7, not terminalization;
7. terminalization uses only pullback stability/composition of surjections;
8. `res=0` means `W∈Z`, and `g=k>0` implies `W>0`.

# 9. Requested review output

Please return one of `PASS`, `INCOMPLETE — Minor Repair`, `INCOMPLETE — Major Revision`, or `COUNTEREXAMPLE / FATAL GAP`; identify the first load-bearing failed implication if not PASS.  In particular, please state whether the four repairs above actually close the previous objections without introducing a new quantifier dependence.