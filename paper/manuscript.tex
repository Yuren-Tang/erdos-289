\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{tikz-cd}
\usepackage{hyperref}
\usepackage{microtype}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\N}{\mathbb N}
\newcommand{\Q}{\mathbb Q}
\newcommand{\Z}{\mathbb Z}
\newcommand{\im}{\operatorname{im}}
\newcommand{\Supp}{\operatorname{Supp}}

\title{Fibrewise Exactification and Reciprocal Interval Profiles\\
\large A categorical proof of Erd\H{o}s Problem 289}
\author{Yuren Tang}
\date{4 September 2026}

\begin{document}
\maketitle

\begin{abstract}
We prove Erd\H{o}s Problem 289 in the stronger subsystem in which every
connected component has size $2$ or $3$.  There exists $d_*$ such that, for
every fixed $d\ge d_*$, all sufficiently large binary component counts $b$
occur in an exact reciprocal representation of $1$ with component profile
$(b_2,b_3)=(b,d)$.

The proof separates categorical exactification from two reciprocal providers.
The target $1$ is centered by the universal quotient $\Q\to\Q/\Z$.  A
source-closed finite Prefix supplies a cofinal affine residue cover with
uniform physical transparency, while a direct late-source Tail uses finite
convolution support at prime simple factors and target-specific filtered
lifting.  Their eventual source tails are intersected, one endpoint source is
frozen before defect and final grade are quantified, and exact value $1$
follows from a pullback of opposite literal cosets in $\Q/\Z$ together with
$0<W<2$.
\end{abstract}

\section{Introduction}

For a finite set $S\subset\N_{>0}$ put
\[
W(S)=\sum_{n\in S}\frac1n.
\]
Erd\H{o}s Problem 289 asks whether, for every sufficiently large integer $k$,
one can find exactly $k$ pairwise disjoint and non-adjacent finite intervals of
positive integers, each of length at least $2$, whose reciprocal weights sum
to $1$.

We regard a finite support in the path graph on $\N_{>0}$ as the physical
state; its intervals are the connected components of the induced graph.  We
work in the stronger subsystem in which every component has size $2$ or $3$.
Write
\[
b_2(S)=\#\{\text{components of size }2\},
\qquad
b_3(S)=\#\{\text{components of size }3\}.
\]
The intrinsic additive label is
\[
\pi(S)=(b_2(S),b_3(S))\in\N^2,
\]
while the ordinary number of intervals is only the consumer
\[
g(S)=b_2(S)+b_3(S).
\]

\begin{theorem}[Fibrewise profile saturation]\label{thm:fibrewise}
There exists $d_*\in\N$ such that for every $d\ge d_*$ there is $b_0(d)$ for
which every $b\ge b_0(d)$ is realized by a finite support $S$ satisfying
\begin{enumerate}
\item every connected component has size $2$ or $3$;
\item distinct components are non-adjacent;
\item $W(S)=1$;
\item $(b_2(S),b_3(S))=(b,d)$.
\end{enumerate}
\end{theorem}

\begin{corollary}[Erd\H{o}s Problem 289]\label{cor:erdos289}
For every sufficiently large integer $k$, there exist exactly $k$ pairwise
disjoint and non-adjacent finite intervals of positive integers, each of
length at least $2$, whose reciprocal weights sum to $1$.
\end{corollary}

\begin{proof}
Fix one $d\ge d_*$.  Theorem~\ref{thm:fibrewise} realizes every sufficiently
large $b$ with profile $(b,d)$, hence every sufficiently large total component
count $b+d$ occurs.
\end{proof}

The active proof graph is
\[
\begin{tikzcd}[column sep=large,row sep=large]
& \text{target quotient / regular images} \arrow[d] & \\
& \text{component-profile specialization} \arrow[dl] \arrow[dr] & \\
\text{source-closed affine ray} \arrow[dr]
&& \text{direct targeted Tail} \arrow[dl] \\
& \text{common endpoint source} \arrow[d] & \\
& \text{opposite-coset pullback} \arrow[d] & \\
& W=1,\quad (b_2,b_3)=(b,d) \arrow[d] & \\
& \text{projection to total component count.} &
\end{tikzcd}
\]

\section{Physical states and categorical bookkeeping}
\label{sec:categorical-bookkeeping}

For a finite support $S$, let
\[
E(S)\rightrightarrows S\longrightarrow\pi_0(S)
\]
be the connected-component quotient of the induced path graph.  Admissibility
requires every component to have cardinality in $\{2,3\}$ and distinct
components to be separated by at least one unused vertex.  Reciprocal weight
and component profile are additive on physically compatible unions.

Write $C^{23}$ for this partial physical state system and
\[
\Sigma^{23}_1
=
\im\bigl(C^{23}_{W=1}\xrightarrow{(b_2,b_3)}\N^2\bigr)
\]
for its exact profile spectrum.  The ordinary grade factors through the strict
triangle
\[
\begin{tikzcd}
C^{23}_{W=1}\arrow[r,"{(b_2,b_3)}"]\arrow[dr,"g"']
&\N^2\arrow[d,"+"]\\
&\N,
\end{tikzcd}
\]
so the ordinary exact-grade spectrum is a regular-image projection of the
joint spectrum.

Only three general categorical operations are used in the active proof.

\paragraph{Pullback of covers.}
Every target or selector requirement that appears in the active proof is
represented by a fibre product.  Surjections in $\mathbf{Set}$, equivalently
regular epimorphisms, remain surjective after pullback.  Thus a family covering
a larger requirement may be restricted to a literal target fibre without
selecting a section.

\paragraph{Compose before reflect.}
Physical compatibility is a property of actual states, not merely of their
aggregate labels.  Literal witness tuples are therefore retained until their
physical union is certified.  Only afterwards do we pass to the regular image
seen by the next consumer.

\paragraph{Support and cardinality.}
For a finite witness family $X\to R$, its observable realizability support is
\[
X\twoheadrightarrow\Supp(X)\hookrightarrow R.
\]
When $R$ is a finite additive group, the same construction admits a cardinality
refinement in $\N[R]$: product families become convolution and the ordinary
regular image is the support of the resulting coefficient function.  Appendix
\ref{app:atomic-convolution} uses this refinement to prove prime-order local
coverage.  Character sums appear only inside the proof of coefficient
positivity and disappear after support is taken.

\section{Target quotient and compact endpoints}
\label{sec:target-quotient}

The target $1$ is the marked morphism
\[
\Z\xrightarrow{1\mapsto1}\Q.
\]
Its universal target-annihilating quotient is the cokernel
\[
A:=\Q/\Z.
\]
For $n\ge1$ put
\[
H_n=n^{-1}\Z/\Z=\ker([n]:A\to A).
\]
These are exactly the finite subgroups of $A$ and
\[
H_n\le H_m\iff n\mid m.
\]

A numerical endpoint cutoff is a different coordinate.  Put
\[
L_B=\operatorname{lcm}(1,\ldots,B),
\qquad
E_B:=H_{L_B}.
\tag{E1}\label{eq:endpoint-subgroup}
\]
Equivalently, $E_B$ is the join of all prime-power current subgroups whose rank
is at most $B$.  The source-index object is simply $\N$ with its ordinary
$B\to\infty$ filter; no further eligibility predicate is used.

The only extra source phenomenon is a boundary predecessor.  Define
\[
\operatorname{Boundary}(B)
=
\{Q=p^e:e\ge3,\ p^{e-1}\le B<p^e\}.
\tag{E2}\label{eq:boundary-definition}
\]
For $Q=p^e\in\operatorname{Boundary}(B)$ put $Q^-=p^{e-1}$.  Then
\[
B<Q=pQ^-\le(Q^-)^2,
\tag{E3}\label{eq:boundary-rank}
\]
because $p\le Q^-$.  Hence boundary predecessors escape uniformly:
for every $q_0$ there is $B_0$ such that
\[
B\ge B_0,\quad Q\in\operatorname{Boundary}(B)
\quad\Longrightarrow\quad
Q^-\ge q_0.
\tag{E4}\label{eq:boundary-escape}
\]
Thus every property holding for all sufficiently large currents also holds,
for all sufficiently large source cutoffs $B$, simultaneously on every
boundary-predecessor row required at that source.  In particular the
$B\to\infty$ filter is proper and finite intersections of eventual source
tails are again eventual and nonempty.

The fixed-source smallness obstruction is already visible here.  For fixed
$H_n$, a positive value $0<W<1$ with zero residue in $A/H_n$ has
$[W]\in H_n$ and therefore satisfies $W\ge1/n$.  Hence the resource tolerance
must be fixed before the eventual endpoint source is chosen.

\input{sections/ltar-provider}
\input{sections/affine-ray-provider}
\input{sections/germ-projections-and-exactification}

\section{Provenance and formalization status}
\label{sec:provenance}

The mathematical route developed independently before the proof claim posted
on 4 September 2026.  A standalone concrete proof manuscript had been
completed in mid-August, followed by categorical reorganization and an ongoing
Lean formalization programme.  The present manuscript uses the later
categorical organization rather than reproducing the older concrete proof
architecture.

The Lean formalization is incomplete and is not claimed here as a formal
verification of the final theorem.  The proof development used substantial
assistance from large language models.  The present text is intended to be
human-checkable; its only external combinatorial theorem is the explicitly
cited independent-transversal theorem.

\bibliographystyle{alpha}
\bibliography{references}

\appendix
\input{appendices/source-closed-prefix-certificate}
\input{appendices/packing}
\input{appendices/prime-supply}

\end{document}
